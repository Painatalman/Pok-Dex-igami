# Copy to .env (and set the same var in the Vercel dashboard for production).
ANTHROPIC_API_KEY=sk-ant-...

# Optional: override the vision model. Defaults to claude-sonnet-5.
IDENTIFY_MODEL=claude-sonnet-5
